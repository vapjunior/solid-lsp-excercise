{
  "name": "vapjunior/toll-crud",
  "description": "CRUD de passagens de pedágio em PHP 8 com LSP, SRP e OCP (SQLite via PDO)",
  "type": "project",
  "license": "MIT",
  "require": {
    "php": ">=8.0"
  },
  "autoload": {
    "psr-4": {
      "App\\": "src/"
    }
  },
  "scripts": {
    "dump": "composer dump-autoload",
    "serve": "php -S localhost:8000 -t public",
    "migrate": "php storage/migrate.php"
  }
}